// eslint-disable-next-line react/prop-types
export default function Header({ children }) {
  return (
    <header className="overflow-hidden " id="home">
      {children}
    </header>
  );
}
